<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.colorful.mapper.hardwareMapper">
    <resultMap id="hardwareMap" type="com.colorful.pojo.Hardware">
        <result column="hardware_id" property="hardwareID"></result>
        <result column="hardware_port" property="hardwarePort"></result>
    </resultMap>
    <insert id="addHardware">
        insert into hardware_data (name,hardware_id,hardware_port,status)
        value (#{name},#{hardware_id},#{hardware_port},1);
    </insert>
    <select id="selectAll" resultMap="hardwareMap" resultType="com.colorful.pojo.Hardware">
        select * from hardware_data;
    </select>

</mapper>

